<UserControl x:Class="TLTDatabaseEditor.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TLTDatabaseEditor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*"/>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="20*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="50*"/>
                <RowDefinition Height="50*"/>
            </Grid.RowDefinitions>

            <ListView Grid.Row="0" ItemsSource="{Binding DBBuildings}" SelectionChanged="BuildingSelectionChangedHandler" SelectionMode="Single"/>

            <ListView Grid.Row="1" ItemsSource="{Binding DBClassrooms}" SelectionChanged="ClassroomSelectionChangedHandler" SelectionMode="Single"/>
        </Grid>

        <Grid Grid.Column="1">
            <ListView ItemsSource="{Binding RoomFeatures}"/>
        </Grid>
        <Grid Grid.Column="2" >
            <Label Content="ROAD"/>
        </Grid>

    </Grid>
</UserControl>
